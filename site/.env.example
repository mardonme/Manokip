# Base URL of the backend API (no trailing slash).
# Local dev: leave empty (Vite proxies /api to localhost:4000).
# Production: set to your Render service URL.
#   VITE_API_URL=https://manokip-server.onrender.com
VITE_API_URL=
