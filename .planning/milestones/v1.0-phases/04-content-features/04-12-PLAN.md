---
phase: 04-content-features
plan: 12
type: execute
wave: 4
depends_on: [04-04, 04-05, 04-06, 04-07, 04-08, 04-09, 04-10, 04-11]
files_modified:
  - tests/e2e/recipe-detail.spec.ts
  - tests/e2e/industry-detail.spec.ts
  - tests/e2e/used-in-section.spec.ts
  - tests/e2e/admin-recipe-form.spec.ts
  - tests/e2e/admin-industry-form.spec.ts
  - .planning/phases/04-content-features/04-VERIFICATION.md
  - .planning/RETROSPECTIVE.md
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06]
user_setup:
  - service: google-rich-results
    why: "DEF-4-12-01 manual gate: validate TechArticle JSON-LD against Google Rich Results Test on Vercel preview"
    dashboard_config:
      - task: "Visit https://search.google.com/test/rich-results"
        location: "Google Search Console testing tool (no account login required)"
  - service: yandex-webmaster
    why: "DEF-4-12-02 manual gate: validate TechArticle JSON-LD against Yandex Webmaster Structured Data Validator (P4-4 risk hedge — industry pages may flag type-mismatch; downgrade to Article in v1.1 if so)"
    dashboard_config:
      - task: "Visit https://webmaster.yandex.com/tools/microtest/ OR Webmaster Console > Структурированные данные"
        location: "Yandex Webmaster (account required for full coverage; public tool sufficient for spot-check)"
must_haves:
  truths:
    - "tests/e2e/recipe-detail.spec.ts has 2 GREEN specs (visitor read + locale-fallback banner) — flipped from test.fixme"
    - "tests/e2e/industry-detail.spec.ts has 2 GREEN specs"
    - "tests/e2e/used-in-section.spec.ts has 2 GREEN specs (visible-when-cross-links + hidden-when-empty + edit-then-refresh propagation)"
    - "tests/e2e/admin-recipe-form.spec.ts has 1 GREEN spec (full author flow)"
    - "tests/e2e/admin-industry-form.spec.ts has 1 GREEN spec"
    - "ZERO test.fixme references remaining in tests/e2e/ for Phase-4 specs (grep returns 0)"
    - "DEF-4-12-01 (Google Rich Results Test for TechArticle on /uz/recipes/<slug> + /uz/industries/<slug>) logged closed-with-deferred-validation per Phase 3 plan 03-09 pattern"
    - "DEF-4-12-02 (Yandex Webmaster Structured Data Validator for both URLs) logged closed-with-deferred-validation"
    - "DEF-4-12-03 (Cyrillic + Uzbek-Latin glyph visual review on recipe + industry body rendering — Inter font subsets per Phase 1 + RESEARCH P9) logged closed-with-deferred-validation"
    - "Per-locale sitemap entries for recipes + industries asserted live (a Wave-0-stub e2e spec or a new sitemap.test.ts probe)"
    - "04-VERIFICATION.md authored summarizing the verification outcome (Phase 3 plan 03-09 pattern)"
    - "RETROSPECTIVE.md appended with Phase 4 cross-milestone trends + lessons"
  artifacts:
    - path: tests/e2e/recipe-detail.spec.ts
      provides: "2 GREEN Playwright specs + JSON-LD shape assertion (script-tag presence + JSON.parse + @type='TechArticle')"
      contains: "TechArticle"
    - path: tests/e2e/industry-detail.spec.ts
      provides: "2 GREEN Playwright specs"
      contains: "TechArticle"
    - path: tests/e2e/used-in-section.spec.ts
      provides: "2 GREEN Playwright specs (cross-link present + hidden + edit-then-refresh propagation)"
      contains: "data-testid=\"used-in-section\""
    - path: tests/e2e/admin-recipe-form.spec.ts
      provides: "1 GREEN spec — admin authors recipe with Tiptap editor + Cloudinary upload smoke + saves + publishes + visits public detail asserts content"
      contains: "loginAsAdminViaDirectToken"
    - path: tests/e2e/admin-industry-form.spec.ts
      provides: "1 GREEN spec — mirror"
      contains: "loginAsAdminViaDirectToken"
    - path: .planning/phases/04-content-features/04-VERIFICATION.md
      provides: "Verification report tallying Wave-0..Wave-3 specs, manual gates closed-with-deferred-validation, requirement coverage CONT-01..06"
      contains: "DEF-4-12-01"
    - path: .planning/RETROSPECTIVE.md
      provides: "Phase 4 retrospective appended with cost trends + Tiptap-v3 lessons + LinkedProductsPicker reuse pattern + UsedInSection cache-fan-out pattern"
      contains: "Phase 4"
  key_links:
    - from: tests/e2e/used-in-section.spec.ts
      to: src/components/public/used-in-section.tsx
      via: "page.goto('/uz/products/<seed-slug>') + locator('[data-testid=\"used-in-section\"]')"
      pattern: "used-in-section"
    - from: tests/e2e/admin-recipe-form.spec.ts
      to: src/actions/recipes.ts
      via: "drives saveRecipe + publishRecipe via the admin UI; asserts visible content on public detail page"
      pattern: "publishRecipe"
---

<objective>
This is the closure plan for Phase 4. It flips Wave-0 e2e stubs from `test.fixme` to live, runs the manual Rich Results / Yandex Webmaster gates as `closed-with-deferred-validation` (Phase 3 plan 03-09 pattern), authors the 04-VERIFICATION.md report, and appends a Phase-4 milestone entry to RETROSPECTIVE.md.

This plan is NOT autonomous — it has 3 manual checkpoint gates (Google Rich Results Test, Yandex Structured Data Validator, Cyrillic+Uzbek-Latin glyph visual review). Each gate is `closed-with-deferred-validation` per the Phase 3 D-09 pattern: the Playwright spec asserts JSON-LD SHAPE (parseable, correct @type, correct properties), but VALIDATION against the actual Google + Yandex parsers is deferred to user-driven post-merge work against a Vercel preview URL. DEF-4-12-01..03 track these outstanding gates; transitions to fully-validated when the user replies with screenshots + Webmaster reports.

Closure status: Closed-with-deferred-validation. The plan's GREEN side is the e2e flip + 04-VERIFICATION.md authoring; the manual gates' validation crosses CLI/UI boundaries the executor cannot drive (RT requires a public URL — Vercel preview) and is logged as DEF entries.

Exit gate: All 8 e2e Phase-4 specs flipped to live + green against Vercel preview (or local dev where applicable per Phase 2 plan 02-17 local-fallback skip pattern); 04-VERIFICATION.md committed; DEF-4-12-01..03 logged.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12.1: Flip recipe/industry detail e2e specs (4 specs)</name>
  <files>tests/e2e/recipe-detail.spec.ts, tests/e2e/industry-detail.spec.ts</files>
  <behavior>
    - tests/e2e/recipe-detail.spec.ts spec 1 (visitor reads): page.goto('/uz/recipes/<seed-recipe-slug>'); expect h1 contains seed title; expect <article class="prose"> contains rendered Tiptap content; expect <script type="application/ld+json"> contains parseable JSON with "@type":"TechArticle"
    - tests/e2e/recipe-detail.spec.ts spec 2 (locale fallback banner): seed a recipe with only `uz` translation present; page.goto('/ru/recipes/<slug>'); expect LocaleFallbackBanner visible with the correct fallback message
    - tests/e2e/industry-detail.spec.ts: 2 specs mirrored
  </behavior>
  <action>
    Flip the 4 fixme specs from `test.fixme` → `test`. Implement the assertions per `<behavior>`. Use seed-content data (assumes a setUp helper has run via the playwright global-setup or per-spec beforeAll).

    JSON-LD shape assertion: `const ld = await page.locator('script[type="application/ld+json"]').last().innerText(); const obj = JSON.parse(ld); expect(obj['@type']).toBe('TechArticle'); expect(obj.headline).toBeDefined(); expect(obj.datePublished).toBeDefined();`

    Locale-fallback assertion: pre-seeding a recipe with only-uz translation requires a fixture helper. If seedRecipe accepts only-one-locale option, use it. If not, extend seedRecipe in tests/fixtures/seed-content.ts opportunistically.
  </action>
  <verify>
    <automated>pnpm playwright test tests/e2e/recipe-detail.spec.ts tests/e2e/industry-detail.spec.ts</automated>
  </verify>
  <done>4 specs green; 0 test.fixme in either file; commit `test(04-12): flip recipe + industry detail e2e to live`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12.2: Flip used-in-section + admin-form e2e specs (4 specs)</name>
  <files>tests/e2e/used-in-section.spec.ts, tests/e2e/admin-recipe-form.spec.ts, tests/e2e/admin-industry-form.spec.ts</files>
  <behavior>
    - used-in-section.spec.ts spec 1 (visible with cross-links): page.goto('/uz/products/<seed-slug-with-cross-links>'); expect locator('[data-testid="used-in-section"]') visible; expect at least 1 RecipeCard + 1 IndustryCard inside
    - used-in-section.spec.ts spec 2 (hidden when 0 cross-links): page.goto('/uz/products/<seed-slug-without-cross-links>'); expect locator('[data-testid="used-in-section"]') NOT visible
    - admin-recipe-form.spec.ts spec (full flow): loginAsAdminViaDirectToken(page); page.goto('/uz/admin/recipes/new'); fill 3-locale tabs (title + slug + excerpt); type into Tiptap editor body; save → redirected to edit; click Publish; navigate to /uz/recipes/<slug>; expect content visible
    - admin-industry-form.spec.ts: same flow mirrored
  </behavior>
  <action>
    Flip the 4 specs. Implement per `<behavior>`. The used-in section component MUST have `data-testid="used-in-section"` attached to its root section element — ADD this in the same task IF not already present (small edit to src/components/public/used-in-section.tsx). The Tiptap editor in admin form MUST be reachable via `[data-testid="recipe-body-editor"]` or `[contenteditable]` — use whichever the component already renders.

    Edit-then-refresh sub-spec OPTIONAL for spec 1 (an additional assertion that links the action layer to the section refresh). If it surfaces flakiness against `next dev` (per Phase 2 plan 02-17 local-fallback skip pattern), use the same `test.skip` guard when CI !== 'true' AND BASE_URL === 'http://localhost:3000'.

    Use Phase-2 plan 02-17's `loginAsAdminViaDirectToken` helper (DB-direct verification token consumption per Pitfall #12). This locks Tiptap editor smoke against the live admin auth path.

    Tiptap editor smoke is BASIC — type "Hello world" + click Save. The `insertImage` flow via Cloudinary widget is harder to drive in Playwright (Cloudinary widget opens an iframe; bypass-able only with a stubbed Cloudinary mock). Defer the image-insert e2e assertion to manual smoke (DEF-4-12-04 if needed). The basic text-edit + save + publish flow is sufficient as the GREEN gate for CONT-01.
  </action>
  <verify>
    <automated>pnpm playwright test tests/e2e/used-in-section.spec.ts tests/e2e/admin-recipe-form.spec.ts tests/e2e/admin-industry-form.spec.ts</automated>
  </verify>
  <done>4 specs green; 0 test.fixme references in any of the 5 Phase-4 e2e files; data-testid="used-in-section" present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12.3: Manual gate — Google Rich Results Test for TechArticle (DEF-4-12-01)</name>
  <what-built>TechArticle JSON-LD emitted on /[locale]/recipes/[slug] + /[locale]/industries/[slug] pages per CONT-06 + D-10. Static-renderer + \\u003c escape closes T-04-XSS-02. JSON-LD shape locked at 04-03 unit specs + flipped e2e specs.</what-built>
  <how-to-verify>
    1. Wait for Vercel preview deploy of the merged Phase-4 PR
    2. Visit https://search.google.com/test/rich-results
    3. Paste the preview URL: `https://manometr-<preview-id>.vercel.app/uz/recipes/<seed-slug>` — assert "Page is eligible for rich results" with TechArticle detected
    4. Paste `https://manometr-<preview-id>.vercel.app/uz/industries/<seed-slug>` — same assertion
    5. Repeat for /ru/recipes/<slug> + /ru/industries/<slug> + /en/recipes/<slug> + /en/industries/<slug> (6 URLs total minimum)
    6. If Google flags any URL with "Type 'TechArticle' is not eligible" or property warnings:
       - For recipes: investigate (recipes ARE the canonical TechArticle subtype; flag is a real issue — likely missing field, fix in v1.0 patch)
       - For industries: per RESEARCH P4-4 / D-10 acceptance, downgrade to '@type': 'Article' in v1.1 (1-line change in techArticleJsonLd helper); log as DEF-4-12-02-resolution-pending
  </how-to-verify>
  <resume-signal>Type "approved" with screenshots/URLs of Rich Results validation, OR "fix-required" with the specific page + warning</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12.4: Manual gate — Yandex Webmaster Structured Data Validator (DEF-4-12-02)</name>
  <what-built>TechArticle JSON-LD on recipe + industry pages. P4-4 risk: Yandex may parse industry-page TechArticle more strictly than Google.</what-built>
  <how-to-verify>
    1. Visit https://webmaster.yandex.com/tools/microtest/ (no Yandex account required for spot-check) OR Yandex Webmaster Console &gt; Структурированные данные (account required, deeper coverage)
    2. Paste preview URL `https://manometr-<preview-id>.vercel.app/uz/recipes/<seed-slug>`
    3. Assert no "Тип данных не распознан" / "Type not recognized" errors; assert TechArticle detected
    4. Paste industry URL → same assertion
    5. If industry URL flags TechArticle as type-mismatch:
       - Log as confirmation of P4-4 risk
       - Plan v1.1 follow-up to switch '@type' to 'Article' for industries (NOT a v1 blocker per D-10 + RESEARCH §A1)
  </how-to-verify>
  <resume-signal>Type "approved" with Yandex screenshots, OR "industry-flags-mismatch" to log v1.1 follow-up</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12.5: Manual gate — Cyrillic + Uzbek-Latin glyph visual review (DEF-4-12-03)</name>
  <what-built>Inter font (Phase 1 SEO-04) loads with subsets ['latin', 'latin-ext', 'cyrillic']. Tiptap body content renders Russian Cyrillic + Uzbek-Latin oʻ/gʻ (U+02BB) per RESEARCH P9.</what-built>
  <how-to-verify>
    1. On Vercel preview: visit /ru/recipes/<seed-slug> with Russian-locale Tiptap body content
    2. Visually inspect: Cyrillic letters render correctly (no fallback font, no question marks, no Tofu boxes)
    3. Visit /uz/recipes/<seed-slug> with Uzbek-Latin body containing oʻ + gʻ characters (U+02BB MODIFIER LETTER TURNED COMMA)
    4. Visually inspect: U+02BB renders as the modifier-letter, not as a quote mark or fallback glyph
    5. Repeat on industries pages
    6. Compare against Phase 3 product detail pages (same Inter font) — should be visually consistent
  </how-to-verify>
  <resume-signal>Type "approved" with sample screenshots, OR "glyph-issue" with the affected page + character</resume-signal>
</task>

<task type="auto">
  <name>Task 12.6: Sitemap entries assertion (live spec or sitemap.test.ts probe)</name>
  <files>tests/e2e/sitemap.spec.ts (extends Phase-3 spec) OR tests/lib/sitemap.test.ts</files>
  <action>
    If `tests/e2e/sitemap.spec.ts` exists from Phase 3 plan 03-08, append 2 assertions: (1) GET /sitemap-uz.xml → contains `<loc>.../recipes/<seed-slug></loc>` for each published seed recipe; (2) same for industries.

    If sitemap is server-side computed (no live route), add a unit test in `tests/lib/sitemap.test.ts` invoking `buildLocaleSitemapEntries('uz')` against seed-content data and asserting recipe + industry entries are present.

    No new file authoring REQUIRED — just append. If both options unavailable, log as DEF-4-12-04 (sitemap visibility gate deferred to Phase 5 launch verification).
  </action>
  <verify>
    <automated>pnpm playwright test tests/e2e/sitemap.spec.ts || pnpm vitest run tests/lib/sitemap.test.ts || echo "sitemap probe deferred to Phase 5"</automated>
  </verify>
  <done>Sitemap recipe + industry entries asserted; or DEF logged for Phase 5.</done>
</task>

<task type="auto">
  <name>Task 12.7: Author 04-VERIFICATION.md + append RETROSPECTIVE.md</name>
  <files>.planning/phases/04-content-features/04-VERIFICATION.md, .planning/RETROSPECTIVE.md</files>
  <action>
    Author `.planning/phases/04-content-features/04-VERIFICATION.md` mirroring `.planning/phases/03-public-rendering-search-seo/03-VERIFICATION.md` shape (or whatever Phase-3 closure file is present). Sections:

    - **Coverage** — table of CONT-01..06 → test files → status
    - **Wave 0 specs landed** — count + file list
    - **Wave 1+ specs flipped GREEN** — count + file list
    - **Manual gates** — DEF-4-12-01..03 entries with closed-with-deferred-validation status + transition criteria (user replies with screenshots / Webmaster reports)
    - **Threats closed** — STRIDE register from this phase + each plan's threat_model + disposition
    - **Deviations** — Rule-1/2/3 logged across the 12 plans
    - **Plan timing** — total time + per-plan times if available
    - **Patterns locked** — TechArticle JSON-LD pattern, LinkedProductsPicker reuse pattern, UsedInSection cache-fan-out pattern, atomic dual-column lifecycle for content tier (mirrors Phase 2 product), Tiptap immediatelyRender: false posture for Next 16

    Append a Phase-4 entry to `.planning/RETROSPECTIVE.md` (Phase-3 closure was the most recent entry):
    - **What worked**: Tiptap v3 install at 3.22.5 lockstep, static-renderer dep-free path, LinkedProductsPicker reuse, schema migration backfill landed cleanly on greenfield-empty tables
    - **What was inefficient**: probably 04-07 admin UI bundle is the long-pole; track for Phase 5 dogfood
    - **Patterns established**: closed-with-deferred-validation for Rich Results parser gates (continuation of Phase-3 pattern); content-tier action shape mirrors product-tier 5-step tx; cap-at-6 v1 trade-off + deferred "all examples" link as v1.1 backlog
    - **Key lessons**: Tiptap v3 immediatelyRender: false IS the difference between working and broken on Next 16 — surfaced at P4-1, mitigated structurally; pgView UNION ALL for Used-In is the single-tag single-read pattern that keeps cache invalidation tractable
    - **Cost patterns**: ~12 plans across 4 waves — Wave 1 + Wave 2 dominate (Server Actions + admin UI); Wave 3 + Wave 4 are bookkeeping
  </action>
  <verify>
    <automated>test -f .planning/phases/04-content-features/04-VERIFICATION.md &amp;&amp; grep -q "Phase 4" .planning/RETROSPECTIVE.md</automated>
  </verify>
  <done>04-VERIFICATION.md authored; RETROSPECTIVE.md appended; commit `docs(04-12): phase 4 verification + retrospective`.</done>
</task>

</tasks>

<dependencies>
- 04-04 — RED stubs that this plan flips to GREEN
- 04-05, 04-06 — Server Actions exercised by admin-form e2e
- 04-07, 04-08 — admin UI required for admin-form e2e
- 04-09, 04-10 — public pages required for detail e2e + JSON-LD assertion
- 04-11 — UsedInSection mounted on product detail required for used-in-section e2e
</dependencies>

<verification>

```bash
pnpm tsc --noEmit
pnpm vitest run                                           # full suite green
pnpm playwright test tests/e2e/recipe-detail.spec.ts tests/e2e/industry-detail.spec.ts tests/e2e/used-in-section.spec.ts tests/e2e/admin-recipe-form.spec.ts tests/e2e/admin-industry-form.spec.ts
! grep -rn "test\.fixme" tests/ | grep -E "(recipe|industry|used-in)"  # zero Phase-4 fixmes remain
ls .planning/phases/04-content-features/04-VERIFICATION.md
grep -q "Phase 4" .planning/RETROSPECTIVE.md
```

After this plan ships, Phase 4 is **LOCALLY COMPLETE** with `closed-with-deferred-validation` posture for DEF-4-12-01..03. The 3 manual gates transition to fully-validated when the user replies with screenshots + Yandex reports + glyph QA.

</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright e2e → Vercel preview deploy | Deployment Protection bypass via VERCEL_AUTOMATION_BYPASS_SECRET threaded by playwright.config.ts (Phase 2 plan 02-17 pattern) |
| Manual gate → Google / Yandex parsers | external service; gate is closed-with-deferred-validation (executor cannot drive parser response from CLI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12-01 | Tampering | Stub e2e specs forgotten + never flipped (false-confidence gap) | mitigate | Tasks 12.1 + 12.2 explicitly flip + assert zero remaining test.fixme references in Phase-4 e2e files. Verification commands include the grep guard. |
| T-04-12-02 | Information Disclosure | Yandex parser flagging industry-page TechArticle as type-mismatch revealing schema decision | accept | P4-4 risk-acknowledged at user level (D-10 LOCKED — proceed with TechArticle for both); v1.1 downgrade plan exists. Yandex acceptance is the trigger, not a blocker. |
| DEF-4-12-01 | Unvalidated assumption | Google Rich Results passes for both recipe + industry URLs in all 3 locales | mitigate via deferred validation | Manual gate; closed-with-deferred-validation; transitions to fully-validated on user reply. |
| DEF-4-12-02 | Unvalidated assumption | Yandex Webmaster passes for both URLs in 3 locales | mitigate via deferred validation | Same posture. |
| DEF-4-12-03 | Unvalidated assumption | Cyrillic + Uzbek-Latin glyphs render correctly across body content | mitigate via deferred validation | Inter font subsets locked Phase 1 SEO-04; visual regression risk near-zero, but verified manually before launch. |

</threat_model>

<deviations_protocol>

**Rule 1**: If during e2e flip a spec discovers an unexpected production-code bug (e.g. UsedInSection rendering an empty `<section>` because data fetch was wrong), fix the production code first, document as Rule-1 deviation, then re-run the spec. Do NOT loosen the spec to match the bug.

**Rule 2**: If the admin-form e2e flow surfaces an opportunity to improve the data-testid coverage (e.g. adding `data-testid="recipe-body-editor"` on the editor wrapper makes the spec more readable), add the attributes opportunistically. UI-attribute additions are Rule-2 acceptance-criterion improvements.

**Rule 3**: All 3 manual gates (12.3, 12.4, 12.5) are `closed-with-deferred-validation` per Phase 3 plan 03-09 + Phase 2 plan 02-17 pattern. The CLI executor cannot drive Google/Yandex parser responses or pixel-level visual review; the gate is the LOCAL test posture (e2e green + JSON-LD shape correct + DEF logged). Transition to fully-validated requires user post-merge action.

</deviations_protocol>
