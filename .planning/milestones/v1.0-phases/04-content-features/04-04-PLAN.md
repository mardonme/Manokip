---
phase: 04-content-features
plan: 04
type: execute
wave: 0
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - tests/fixtures/seed-content.ts
  - tests/fixtures/tiptap-malicious.ts
  - tests/fixtures/tiptap-sample.ts
  - tests/actions/recipes.test.ts
  - tests/actions/industries.test.ts
  - tests/components/recipe-form.test.tsx
  - tests/components/industry-form.test.tsx
  - tests/components/linked-products-picker.test.tsx
  - tests/e2e/recipe-detail.spec.ts
  - tests/e2e/industry-detail.spec.ts
  - tests/e2e/used-in-section.spec.ts
  - tests/e2e/admin-recipe-form.spec.ts
  - tests/e2e/admin-industry-form.spec.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]
must_haves:
  truths:
    - "tests/fixtures/seed-content.ts extends seed-public from Phase 3 with 2 published recipes + 2 published industries + cross-links to existing 6 seed products"
    - "tests/fixtures/tiptap-malicious.ts + tiptap-sample.ts ship reusable JSONContent fixtures consumed by Wave 0/1+ specs"
    - "tests/actions/recipes.test.ts has 7 RED specs (test.fixme or describe.skip) covering saveRecipe / publishRecipe / unpublishRecipe / deleteRecipe / saveProductRecipes replace-on-save"
    - "tests/actions/industries.test.ts mirrored with 7 RED specs"
    - "tests/components/recipe-form.test.tsx + industry-form.test.tsx + linked-products-picker.test.tsx ship as RED jsdom stubs (test.fixme)"
    - "tests/e2e/{recipe-detail,industry-detail,used-in-section,admin-recipe-form,admin-industry-form}.spec.ts ship as test.fixme stubs flipped to live in Wave 1+ / Wave 4"
  artifacts:
    - path: tests/fixtures/seed-content.ts
      provides: "seedRecipe + seedIndustry + seedProductRecipes + seedProductIndustries fixture helpers + seedPhase4Content() that orchestrates 2 recipes + 2 industries + cross-links + builds on seed-public"
      contains: "seedPhase4Content"
    - path: tests/fixtures/tiptap-malicious.ts
      provides: "MALICIOUS_TIPTAP_DOC fixture (script tag + img onerror + closing-tag-break) for XSS regression locking"
      contains: "alert(1)"
    - path: tests/fixtures/tiptap-sample.ts
      provides: "SAMPLE_RECIPE_DOC + SAMPLE_INDUSTRY_DOC well-formed multi-block JSONContent fixtures"
      contains: "type: 'doc'"
    - path: tests/actions/recipes.test.ts
      provides: "7 RED stubs locking saveRecipe + lifecycle + replace-on-save contracts (flip to GREEN in 04-05)"
      contains: "test.fixme"
    - path: tests/actions/industries.test.ts
      provides: "7 RED stubs (flip to GREEN in 04-06)"
      contains: "test.fixme"
    - path: tests/components/recipe-form.test.tsx
      provides: "jsdom RED stub for recipe-form behavior (flip in 04-07)"
      contains: "test.fixme"
    - path: tests/components/industry-form.test.tsx
      provides: "jsdom RED stub (flip in 04-08)"
      contains: "test.fixme"
    - path: tests/components/linked-products-picker.test.tsx
      provides: "jsdom RED stub (flip in 04-07)"
      contains: "LinkedProductsPicker"
    - path: tests/e2e/recipe-detail.spec.ts
      provides: "Playwright RED stubs (visitor reads recipe + locale-fallback banner) — flipped 04-12"
      contains: "test.fixme"
    - path: tests/e2e/industry-detail.spec.ts
      provides: "Playwright RED stubs — flipped 04-12"
      contains: "test.fixme"
    - path: tests/e2e/used-in-section.spec.ts
      provides: "Playwright RED stubs (Used-in present + hidden when 0 cross-links + edit-then-refresh propagation) — flipped 04-12"
      contains: "test.fixme"
    - path: tests/e2e/admin-recipe-form.spec.ts
      provides: "Playwright RED stub for admin recipe authoring smoke — flipped 04-12"
      contains: "test.fixme"
    - path: tests/e2e/admin-industry-form.spec.ts
      provides: "Playwright RED stub — flipped 04-12"
      contains: "test.fixme"
  key_links:
    - from: tests/fixtures/seed-content.ts
      to: tests/fixtures/seed-public.ts
      via: "imports seedPublicCatalog or its product helpers from Phase 3"
      pattern: "from '@/tests/fixtures/seed-public'"
    - from: tests/actions/recipes.test.ts
      to: tests/fixtures/seed-content.ts
      via: "imports seedRecipe + seedProductRecipes for setUp"
      pattern: "seedRecipe"
---

<objective>
This plan ships test stubs (RED) and seed fixtures BEFORE the Wave 1+ implementation lands. This is the project-standard TDD posture (Phase 2 + Phase 3 every plan): the test exists first, fails because the production module is undefined, then the implementation lands as the GREEN side of the cycle.

Three categories of test stubs ship here:
1. **Live-Neon integration tests** for Server Actions (04-05/04-06 will write the actions and flip these to GREEN by removing `test.fixme`)
2. **jsdom component tests** for admin form components (flipped in 04-07/04-08)
3. **Playwright e2e tests** for visitor reads + admin author flow + Used-In integration (flipped in 04-12 closure plan)

The seed-content fixture extends seed-public from Phase 3 (the existing 6-product trilingual catalog) with 2 published recipes + 2 published industries + cross-links — gives downstream specs realistic data without per-test seeding overhead.

Closure status: Standard execute plan. All test files ship as RED — `pnpm vitest run` MUST list the new test files as containing fixme'd specs (the count goes up but pass rate stays 100%). Exit gate: each test file enumerated by `pnpm vitest run --list` AND `pnpm playwright test --list`.
</objective>

<tasks>

<task type="auto">
  <name>Task 4.1: Tiptap doc fixtures (malicious + sample)</name>
  <files>tests/fixtures/tiptap-malicious.ts, tests/fixtures/tiptap-sample.ts</files>
  <action>
    Author `tests/fixtures/tiptap-malicious.ts` per RESEARCH §Code Examples lines 1048-1067 verbatim — `MALICIOUS_TIPTAP_DOC` with `<script>alert(1)</script>` text + bold-marked `</p><img src=x onerror=alert(2)>` text + a "Normal text after." paragraph.

    Author `tests/fixtures/tiptap-sample.ts` exporting `SAMPLE_RECIPE_DOC` + `SAMPLE_INDUSTRY_DOC` — both well-formed JSONContent with 1 H2 + 2 paragraphs + 1 link mark + 1 image node with `attrs.publicId: 'manometr/sample/inline-1'` (dummy public_id; Cloudinary returns 404 but fixture only used in unit/integration tests, never deployed).

    Both fixtures have `type JSONContent` import from `@tiptap/core`. Pure data, no React, no test runtime.
  </action>
  <verify>
    <automated>test -f tests/fixtures/tiptap-malicious.ts &amp;&amp; test -f tests/fixtures/tiptap-sample.ts &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | grep -E "tests/fixtures/tiptap" || echo "fixtures typecheck"</automated>
  </verify>
  <done>Both fixture files present + exported; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4.2: seed-content fixture orchestrator (extends seed-public)</name>
  <files>tests/fixtures/seed-content.ts</files>
  <action>
    Author `tests/fixtures/seed-content.ts` exporting:
    - `seedRecipe(tx, { id?, status?, publishedAt?, featuredImagePublicId?, translations: { uz, ru, en } })` — inserts recipe + 3 translation rows; returns recipe id
    - `seedIndustry(tx, ...)` — same shape for industry
    - `seedProductRecipes(tx, productId, recipeIds: string[], position?: number)` — inserts junction rows (position auto-increments)
    - `seedProductIndustries(tx, ...)` — same shape
    - `seedPhase4Content(tx, { products?: string[] })` — orchestrator: assumes products exist (caller seeds via seed-public from Phase 3 first); seeds 2 published recipes + 2 published industries + cross-links each recipe to 2 products + each industry to 1 product

    Use SAMPLE_RECIPE_DOC / SAMPLE_INDUSTRY_DOC from task 4.1 for body content. Trilingual translations follow the Phase-3 seed-public convention (uz prefixed with "uz_", ru prefixed with "ru_", en prefixed with "en_") — this is the existing test-fixture posture and downstream specs grep on the prefix.

    Note: do NOT export this file from a barrel — fixtures import directly via path.
  </action>
  <verify>
    <automated>test -f tests/fixtures/seed-content.ts &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | grep -E "tests/fixtures/seed-content" || echo "seed fixture typecheck"</automated>
  </verify>
  <done>seedRecipe + seedIndustry + seedProductRecipes + seedProductIndustries + seedPhase4Content exported; tsc clean; references existing seed-public products via productId param.</done>
</task>

<task type="auto">
  <name>Task 4.3: Server Action RED test stubs (recipes + industries)</name>
  <files>tests/actions/recipes.test.ts, tests/actions/industries.test.ts</files>
  <action>
    Author `tests/actions/recipes.test.ts` with 7 specs — ALL marked `test.fixme(...)` so they enumerate but skip. The 7 specs (per RESEARCH §Plan structure recommendation Wave 1):

    1. saveRecipe creates a new recipe with 3-locale translations + 0 linked products → recipe row + 3 translation rows present + 0 junction rows
    2. saveRecipe updates an existing recipe with replaced linked products array → DELETE+INSERT replace-on-save, junction rows match input array, position field set
    3. saveRecipe with status='published' but persisted status='draft' → throws USE_PUBLISH_ACTION (W7 refusal-to-elevate, Phase 2 D-11 pattern)
    4. publishRecipe atomic dual-column write → status='published' + publishedAt=now() in ONE SET clause; audit row action='publish' entityType='recipe'
    5. unpublishRecipe atomic dual-column write → status='draft' + publishedAt=null; audit row action='unpublish'
    6. deleteRecipe audit-before-delete → audit row captured BEFORE delete; FK cascade drops recipe_translations + product_recipes
    7. saveRecipe revalidates "Used in" for old + new linked products (union) → revalidateUsedIn called for every productId in (oldLinkedProductIds ∪ newLinkedProductIds)

    Each spec is a `test.fixme('description', async () => { /* skeleton */ })` that imports `saveRecipe` etc. from a not-yet-existing `@/actions/recipes` module — the import failure is part of RED.

    Mirror for `tests/actions/industries.test.ts` — 7 specs, same shape, swapped entity.

    These flip to live in 04-05/04-06 by removing `test.fixme` → `test`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/actions/recipes.test.ts tests/actions/industries.test.ts --reporter=verbose 2>&amp;1 | grep -E "skipped|fixme" || echo "stubs enumerate as fixme"</automated>
  </verify>
  <done>14 fixme specs across 2 files; each spec has a skeleton import + describe block + it/test.fixme; commit message tags TDD-RED.</done>
</task>

<task type="auto">
  <name>Task 4.4: jsdom + Playwright e2e RED stubs</name>
  <files>tests/components/recipe-form.test.tsx, tests/components/industry-form.test.tsx, tests/components/linked-products-picker.test.tsx, tests/e2e/recipe-detail.spec.ts, tests/e2e/industry-detail.spec.ts, tests/e2e/used-in-section.spec.ts, tests/e2e/admin-recipe-form.spec.ts, tests/e2e/admin-industry-form.spec.ts</files>
  <action>
    For EACH of the 8 files, ship a minimal RED stub:
    - **Component tests (3 files)**: `test.fixme('renders 3-locale tabs and swaps content', ...)`, `test.fixme('LinkedProductsPicker filters options client-side', ...)`, etc. — at least 1 fixme spec per file with import skeleton.
    - **Playwright specs (5 files)**: `test.fixme('visitor reads /uz/recipes/<slug> with prose body', ...)`, `test.fixme('locale fallback shows banner', ...)`, `test.fixme('used-in section visible with 2 cross-links', ...)`, `test.fixme('used-in section hidden when 0 cross-links', ...)`, `test.fixme('admin authors recipe with Tiptap editor + insertImage', ...)`, etc.

    Per RESEARCH §Phase Requirements → Test Map (lines 985-997), the test count distribution is:
    - tests/e2e/recipe-detail.spec.ts: 2 specs (visitor read + locale fallback banner)
    - tests/e2e/industry-detail.spec.ts: 2 specs (visitor read + locale fallback banner)
    - tests/e2e/used-in-section.spec.ts: 2 specs (visible + hidden)
    - tests/e2e/admin-recipe-form.spec.ts: 1 spec (author + save + publish + visit public detail asserts content)
    - tests/e2e/admin-industry-form.spec.ts: 1 spec (same)
    - tests/components/recipe-form.test.tsx: 2 specs (3-locale tab swap + Tiptap mount)
    - tests/components/industry-form.test.tsx: 2 specs (mirror)
    - tests/components/linked-products-picker.test.tsx: 2 specs (filter + multi-select)

    Total: 14 fixme specs across 8 files.

    Add a `// FLIP-IN: 04-NN-PLAN` comment at the top of each fixme spec body so the plan that owns flipping has a grep target.

    All Playwright specs include the standard pre-imports: `import { test, expect } from '@playwright/test'` + (for admin specs) the DB-direct verification token consumption helper from `tests/fixtures/admin-magic-link.ts` (Phase 2 plan 02-17 pattern). Authentication helper signature: `await loginAsAdminViaDirectToken(page)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/recipe-form.test.tsx tests/components/industry-form.test.tsx tests/components/linked-products-picker.test.tsx --reporter=verbose 2>&amp;1 | grep -E "skipped|fixme|6 (skipped|fixme)" &amp;&amp; pnpm playwright test --list tests/e2e/recipe-detail.spec.ts tests/e2e/industry-detail.spec.ts tests/e2e/used-in-section.spec.ts tests/e2e/admin-recipe-form.spec.ts tests/e2e/admin-industry-form.spec.ts</automated>
  </verify>
  <done>All 8 files exist; vitest enumerates 6 jsdom fixme specs; playwright --list enumerates 8 fixme e2e specs; all marked with FLIP-IN comments referencing the plan that flips them.</done>
</task>

</tasks>

<dependencies>
- 04-01 — schema migration (seed fixtures need junction tables)
- 04-02 — Tiptap deps (fixtures import JSONContent type)
- 04-03 — lib helpers (test imports reference @/lib/recipes, @/lib/industries, etc., even if fixme'd)
</dependencies>

<verification>

```bash
pnpm tsc --noEmit
pnpm vitest run --reporter=verbose 2>&1 | grep -cE "fixme|skipped"   # expect ≥20 (14 actions + 6 components)
pnpm playwright test --list 2>&1 | grep -cE "fixme"                  # expect ≥8
```

After this plan ships, Wave 0 is complete. The complete RED test inventory is enumerated; Wave 1 plans flip in their respective specs as features land.

</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → tests/fixtures/* | trusted (test-only code; fixtures contain dummy IDs, never deployed) |
| MALICIOUS_TIPTAP_DOC fixture | adversarial-by-design (regression-locker payloads); never written to live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | seed-content fixture overwriting Phase-3 seed-public products | mitigate | seed-content takes a `products: string[]` param; never inserts new products. seedPhase4Content asserts caller has seeded base products via seed-public first. |
| T-04-04-02 | DoS | RED stubs forgotten + never flipped to live (false-confidence in test count) | mitigate | Each fixme spec carries a `// FLIP-IN: 04-NN-PLAN` comment with grep target. Wave 4 closure plan 04-12 verification command greps for orphaned fixmes (`grep -rn "FLIP-IN" tests/` should return 0 lines after Wave 4 closes). |

</threat_model>

<deviations_protocol>

**Rule 1**: If `test.fixme` is not the right Vitest API (different from Playwright's `test.fixme`), use the equivalent: Vitest 4 supports `test.skip` + `it.skip` for the same purpose; Playwright supports `test.fixme`. Both must enumerate but not run.

**Rule 2**: If during stub authoring a spec discovers a missing fixture helper (e.g. `seedRecipeWithLinkedProducts` convenience wrapper), add it to `tests/fixtures/seed-content.ts` opportunistically. Fixture surface is iteratively expanded.

**Rule 3**: If `tests/fixtures/admin-magic-link.ts` from Phase 2 plan 02-17 doesn't exist with the exact `loginAsAdminViaDirectToken` signature expected here, look up the actual export and adapt the Playwright stubs to match. The DB-direct verification token consumption pattern IS landed (verified in 02-17 SUMMARY) — only the export name may differ.

</deviations_protocol>
