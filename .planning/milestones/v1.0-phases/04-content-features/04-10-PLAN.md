---
phase: 04-content-features
plan: 10
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-06, 04-09]
files_modified:
  - src/app/[locale]/industries/page.tsx
  - src/app/[locale]/industries/[slug]/page.tsx
  - src/components/public/industry-card.tsx
  - src/lib/sitemap.ts
autonomous: true
requirements: [CONT-03, CONT-06]
must_haves:
  truths:
    - "/[locale]/industries index page renders all published industries as a card grid"
    - "/[locale]/industries/[slug] detail page renders industry with Tiptap body via renderTiptapToHtml + locale fallback banner"
    - "TechArticle JSON-LD emitted for industry pages (D-10 LOCKED — even though Article would be the strict semantic fit per RESEARCH P4-4, ship as TechArticle for v1; manual gate in 04-12 validates Yandex acceptance)"
    - "buildAlternates emits hreflang per locale + x-default; per-locale canonical"
    - "src/lib/sitemap.ts extended with industry entries (mirror of recipe sitemap extension from 04-09)"
    - "IndustryCard reused by 04-11 Used-In section (mirror of RecipeCard pattern)"
  artifacts:
    - path: src/app/[locale]/industries/page.tsx
      provides: "Industries list page (RSC)"
      contains: "findPublishedIndustries"
    - path: src/app/[locale]/industries/[slug]/page.tsx
      provides: "Industry detail page (RSC + TechArticle JSON-LD + Tiptap static-render)"
      contains: "techArticleJsonLd"
    - path: src/components/public/industry-card.tsx
      provides: "Card for the index grid + Used-In section reuse"
      contains: "CldImage"
    - path: src/lib/sitemap.ts
      provides: "Sitemap helper extended with industry entries"
      contains: "industries"
  key_links:
    - from: src/app/[locale]/industries/[slug]/page.tsx
      to: src/lib/industries.ts
      via: "getIndustryBySlug(slug, locale)"
      pattern: "getIndustryBySlug"
    - from: src/app/[locale]/industries/[slug]/page.tsx
      to: src/lib/tiptap-render.ts
      via: "renderTiptapToHtml(industry.translation.body)"
      pattern: "renderTiptapToHtml"
    - from: src/app/[locale]/industries/[slug]/page.tsx
      to: src/lib/jsonld.ts
      via: "techArticleJsonLd({ ... })"
      pattern: "techArticleJsonLd"
---

<objective>
This plan ships the public industry-rendering surface — mirror of plan 04-09 with industry entity. Same TechArticle JSON-LD posture (D-10 LOCKED, even though Article would be the strict semantic fit per RESEARCH P4-4 / A1 — ship TechArticle for both, manual gate in 04-12 validates Yandex acceptance, downgrade to Article in v1.1 if the Webmaster report flags type-mismatch).

LocaleFallbackBanner reused verbatim from 04-09 (component already accepts entityType prop). RecipeCard pattern duplicated for IndustryCard — both have identical shape (featured image + title + excerpt + link), only entity-name differs. The duplication is intentional: each card type evolves independently if industry-specific badges or affordances are added in v1.1 (e.g. industry pages might show a "vertical" facet tag, recipes might show a "difficulty" tag).

Closure status: Standard execute plan. No new test files; e2e flips in 04-12. Exit gate: tsc clean + visit /uz/industries + /uz/industries/<seed-slug> in browser.
</objective>

<tasks>

<task type="auto">
  <name>Task 10.1: IndustryCard primitive</name>
  <files>src/components/public/industry-card.tsx</files>
  <action>
    Author `src/components/public/industry-card.tsx` mirroring `src/components/public/recipe-card.tsx` from 04-09 verbatim — change `recipe` → `industry`, link href to /[locale]/industries/[slug]. Same CldImage 16:9 + sizes attribute + Phase-3 typography pattern.
  </action>
  <verify>
    <automated>test -f src/components/public/industry-card.tsx &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | grep -E "industry-card" || echo "industry-card typechecks"</automated>
  </verify>
  <done>IndustryCard exported; props mirror RecipeCard with industry entity.</done>
</task>

<task type="auto">
  <name>Task 10.2: /[locale]/industries index + detail pages + sitemap extension</name>
  <files>src/app/[locale]/industries/page.tsx, src/app/[locale]/industries/[slug]/page.tsx, src/lib/sitemap.ts</files>
  <action>
    Mirror plan 04-09 Tasks 9.2 + 9.3 verbatim with industry entity:
    - /[locale]/industries/page.tsx: findPublishedIndustries(locale); render <IndustryCard> grid; cacheTag(`industries:list:${locale}`); generateMetadata with buildAlternates
    - /[locale]/industries/[slug]/page.tsx: getIndustryBySlug; LocaleFallbackBanner if usedFallbackLocale; hero CldImage; <article className="prose prose-slate"> Tiptap body; TechArticle JSON-LD with mentions array; cacheTag(`industry:${id}`)
    - src/lib/sitemap.ts: extend buildLocaleSitemapEntries with industry entries (mirrors recipe extension landed in 04-09)

    Translation-strings additions: `Industries.index.{title, subtitle, empty}` + `LocaleFallback.industry` namespace in messages/{uz,ru,en}.json — mirror Recipes namespace from 04-09.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | grep -E "src/app/\[locale\]/industries" || echo "industries pages typecheck" &amp;&amp; grep -q "industries" src/lib/sitemap.ts</automated>
  </verify>
  <done>Index + detail pages exported; TechArticle JSON-LD with \\u003c escape; sitemap.ts emits industry entries per locale; messages files updated.</done>
</task>

</tasks>

<dependencies>
- 04-02 — Tiptap deps + renderTiptapToHtml
- 04-03 — getIndustryBySlug + findPublishedIndustries + techArticleJsonLd
- 04-06 — Server Actions (industries authorable for seed data)
- 04-09 — LocaleFallbackBanner primitive (reused) + sitemap.ts already extended once for recipes (industries adds parallel block)
</dependencies>

<verification>

```bash
pnpm tsc --noEmit
pnpm next build
# After dev seed: visit /uz/industries + /uz/industries/<slug>
# View source: <script type="application/ld+json"> with "@type":"TechArticle"
```

After this plan ships, both public content surfaces are online. Wave 3 closes when 04-11 mounts the Used-In section.

</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor browser ← /[locale]/industries/[slug] | RSC-rendered HTML; no client ProseMirror bundle |
| TechArticle JSON-LD ← public visitor | \\u003c escape on serialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-XSS-01 | XSS | Industry body rendered to HTML | mitigate | Static-renderer + escapeHTML (same as 04-09); body never bypasses TIPTAP_EXTENSIONS allow-list. |
| T-04-XSS-02 | XSS | TechArticle JSON-LD escape | mitigate | \\u003c escape on JSON.stringify (same as 04-09). |
| T-04-INFO-01 | Information Disclosure | Public read serving draft industries | mitigate | getIndustryBySlug + findPublishedIndustries filter WHERE industries.status='published'. |
| P4-4 | Schema mismatch | Yandex parses TechArticle on industry page as type-mismatch | accept-with-deferred-validation | Manual gate DEF-4-12-02 in plan 04-12 — Rich Results Test for both Google + Yandex; downgrade to Article in v1.1 if flagged. |

</threat_model>

<deviations_protocol>

**Rule 1**: If sitemap.ts edits from 04-09 + 04-10 conflict in parallel-merge scenario, resolve by including BOTH the recipe-entry + industry-entry blocks. Structural overlap, not logic conflict.

**Rule 2**: If during integration, the LocaleFallbackBanner needs an industry-specific message variant (e.g. "vertical landing pages should warn more loudly because partial translations look amateurish"), expand the message set without breaking the recipe variant. The component's `entityType` prop selects the variant.

**Rule 3**: If `pnpm next build` fails on cacheComponents for the new industry pages, the rootParams: true fix from Phase 3 commit 2c63c24 is already shipped — no per-page change needed.

</deviations_protocol>
