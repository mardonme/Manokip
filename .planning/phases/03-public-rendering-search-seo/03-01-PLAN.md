---
phase: 03-public-rendering-search-seo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/locale-switcher.spec.ts
  - tests/e2e/category-nav.spec.ts
  - tests/e2e/catalog-filters.spec.ts
  - tests/e2e/product-detail.spec.ts
  - tests/e2e/manufacturers.spec.ts
  - tests/db/catalog.test.ts
  - tests/db/search.test.ts
  - tests/lib/jsonld.test.ts
  - tests/lib/metadata.test.ts
  - tests/api/autocomplete.test.ts
  - tests/api/sitemap.test.ts
  - tests/actions/products.test.ts
  - tests/fixtures/seed-public.ts
  - next.config.ts
  - package.json
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-07, CAT-08, SRCH-01, SRCH-02, SRCH-03, SRCH-05, MFG-01, MFG-02, SEO-01, SEO-02, SEO-03]
must_haves:
  truths:
    - "Every Phase-3 requirement has a corresponding RED-state test stub or fixture seeded under tests/"
    - "next.config.ts has cacheComponents: true enabling the 'use cache' + cacheTag pipeline"
    - "schema-dts is installed so src/lib/jsonld.ts can import schema.org TypeScript types"
    - "tests/fixtures/seed-public.ts provides a deterministic seed of 3 manufacturers + 6 products in 2 categories with all 3 locales for downstream e2e + integration tests"
    - "tests/actions/products.test.ts has SRCH-05 describe.skip stub appended for the tsvector-rebuild assertion that Plan 02 will flip to GREEN"
  artifacts:
    - path: "tests/fixtures/seed-public.ts"
      provides: "Deterministic public-side seed: 3 manufacturers, 6 published products in 2 categories, 3 locales each, ≥1 numeric spec field, ≥1 enum spec field"
      exports: ["seedPublicFixture", "PublicFixtureIds"]
    - path: "tests/lib/jsonld.test.ts"
      provides: "Stub asserting productJsonLd / organizationJsonLd / breadcrumbJsonLd / collectionPageJsonLd shapes"
      contains: "describe.skip"
    - path: "tests/lib/metadata.test.ts"
      provides: "Stub for SEO-01/SEO-02 hreflang + canonical generator"
      contains: "describe.skip"
    - path: "tests/db/catalog.test.ts"
      provides: "Stubs for CAT-03 product count and CAT-04 EAV filter behavior"
      contains: "describe.skip"
    - path: "tests/db/search.test.ts"
      provides: "Stubs for SRCH-01 ranked results and SRCH-02 locale fallback cascade"
      contains: "describe.skip"
    - path: "tests/api/autocomplete.test.ts"
      provides: "Stub for SRCH-03 prefix + SKU autocomplete"
      contains: "describe.skip"
    - path: "tests/api/sitemap.test.ts"
      provides: "Stub for SEO-03 per-locale sitemap XML"
      contains: "describe.skip"
    - path: "tests/actions/products.test.ts"
      provides: "Extended with SRCH-05 describe.skip stub — tsvector rebuild on saveProduct (Plan 02 flips to GREEN)"
      contains: "describe.skip('SRCH-05"
    - path: "tests/e2e/locale-switcher.spec.ts"
      provides: "Stub for CAT-01 locale switcher"
      contains: "test.skip"
    - path: "tests/e2e/category-nav.spec.ts"
      provides: "Stub for CAT-02 category tree"
      contains: "test.skip"
    - path: "tests/e2e/catalog-filters.spec.ts"
      provides: "Stub for CAT-05 URL-state filters"
      contains: "test.skip"
    - path: "tests/e2e/product-detail.spec.ts"
      provides: "Stubs for CAT-06 spec tables, CAT-07 SSR HTML, SRCH-04 SKU 302"
      contains: "test.skip"
    - path: "tests/e2e/manufacturers.spec.ts"
      provides: "Stubs for MFG-01 index + MFG-02 detail"
      contains: "test.skip"
    - path: "next.config.ts"
      provides: "cacheComponents: true flag enabling 'use cache' + cacheTag throughout Phase 3"
      contains: "cacheComponents: true"
  key_links:
    - from: "tests/db/catalog.test.ts"
      to: "tests/fixtures/seed-public.ts"
      via: "import { seedPublicFixture } from '../fixtures/seed-public'"
      pattern: "seed-public"
    - from: "All Phase 3 plans"
      to: "next.config.ts cacheComponents: true"
      via: "'use cache' directive presence"
      pattern: "cacheComponents"
    - from: "tests/actions/products.test.ts SRCH-05 stub"
      to: "Plan 02 Task 2.3 (tsvector rebuild)"
      via: "describe.skip → describe flip"
      pattern: "SRCH-05"
---

<objective>
Wave 0 lands the test scaffolding, the deterministic public-side seed fixture, the Next.js 16 `cacheComponents` flag, and the `schema-dts` install — everything downstream Phase-3 plans depend on. No production source files are touched.

Purpose: every CAT-/SRCH-/MFG-/SEO- requirement gets a RED test stub up front so each downstream plan flips its targeted stub from RED→GREEN. `cacheComponents: true` must precede any `'use cache'` use (Pitfall #1, RESEARCH.md). `schema-dts` must be installed before plan 03 imports it.
Output: 12 test stub files (1 of which is an extension to the existing tests/actions/products.test.ts for SRCH-05), 1 fixture file, `cacheComponents: true` in `next.config.ts`, `schema-dts` in `dependencies`.
</objective>

<execution_context>
@C:/Users/hp elitebook/OneDrive/Desktop/Manometr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hp elitebook/OneDrive/Desktop/Manometr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-rendering-search-seo/03-CONTEXT.md
@.planning/phases/03-public-rendering-search-seo/03-RESEARCH.md
@.planning/phases/03-public-rendering-search-seo/03-PATTERNS.md
@.planning/phases/03-public-rendering-search-seo/03-VALIDATION.md
@CLAUDE.md
@next.config.ts
@package.json

<interfaces>
<!-- next-intl LOCALES — used by every test fixture and stub -->
From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['uz', 'ru', 'en'],
  defaultLocale: 'uz',
  localePrefix: 'always',
});
```

<!-- Drizzle DB client — used by seed-public fixture -->
From src/db/client.ts:
```typescript
export const db: NeonHttpDatabase;
```

<!-- Existing seed-products fixture (Phase 2) — closest analog for seed-public -->
From tests/fixtures/seed-products.ts:
```typescript
export async function seedProductFixture(): Promise<{ productIds, categoryId, manufacturerId, specFieldIds }>;
```

<!-- Schema imports for fixture -->
From src/db/schema/index.ts:
```typescript
export const categories, categoryTranslations,
  manufacturers, manufacturerTranslations,
  products, productTranslations,
  specFields, specFieldGroups,
  productSpecValues, productSpecValueTranslations;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Install schema-dts + enable cacheComponents</name>
  <files>package.json, next.config.ts</files>
  <read_first>
    - next.config.ts (current state — must preserve withSentryConfig + withNextIntl + turbopack.root)
    - package.json (current dependency block — pnpm 10.33.0)
    - .planning/phases/03-public-rendering-search-seo/03-RESEARCH.md §"Pitfall 1: 'use cache' not enabled in next.config.ts"
    - .planning/phases/03-public-rendering-search-seo/03-PATTERNS.md §"'use cache' + cacheTag ISR data-fetch pattern"
  </read_first>
  <action>
    1. Run `pnpm add schema-dts` — install at latest semver-compatible v1.x. Verify it lands under dependencies (not devDependencies) since `src/lib/jsonld.ts` is imported by RSC pages.
    2. Edit `next.config.ts` — inside the `nextConfig: NextConfig = { ... }` object, ADD `cacheComponents: true,` as a top-level key (NOT inside `experimental`; it became stable in Next.js 16 per RESEARCH.md §"State of the Art"). Place the flag immediately after the `turbopack` block. Keep `turbopack.root`, `images.remotePatterns`, and the `withSentryConfig(withNextIntl(...))` wrapping unchanged.
    3. Run `pnpm tsc --noEmit` to confirm the type for `NextConfig` accepts `cacheComponents: true`.
    4. Run `pnpm build` once to confirm the production build still compiles with the flag enabled (Pitfall A6 — admin pages use force-dynamic so should bypass the new cache).
  </action>
  <verify>
    <automated>grep -q '"schema-dts"' package.json &amp;&amp; grep -q 'cacheComponents: true' next.config.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"schema-dts":' package.json` exits 0
    - `grep -nE 'cacheComponents:\s*true' next.config.ts` returns one match
    - `next.config.ts` retains `withSentryConfig(withNextIntl(nextConfig), { ... })` as the export and the `turbopack: { root: path.resolve(__dirname) }` block
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0 (the admin routes still build under the new flag)
  </acceptance_criteria>
  <done>
    schema-dts installed; cacheComponents: true active; production build green.
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Author seed-public fixture (3 manufacturers, 6 products, 2 categories, 3 locales)</name>
  <files>tests/fixtures/seed-public.ts</files>
  <read_first>
    - tests/fixtures/seed-products.ts (closest analog — Phase 2 seedProductFixture, copy the deterministic-IDs + tx pattern)
    - src/db/schema/products.ts (column shape including new image/datasheet arrays — but ARRAYS NOT YET IN SCHEMA in Wave 0; fixture must NOT write image_public_ids / datasheet_public_ids — those columns land in plan 02)
    - src/db/schema/manufacturers.ts (column shape; is_official_rep also lands in plan 02 — fixture must not set it)
    - src/db/schema/categories.ts
    - src/db/schema/spec-fields.ts + src/db/schema/spec-values.ts
    - .planning/phases/03-public-rendering-search-seo/03-VALIDATION.md (fixture spec — 3 manufacturers, 6 products in 2 categories, 3 locales each)
  </read_first>
  <action>
    Create `tests/fixtures/seed-public.ts` exporting:

    ```typescript
    export interface PublicFixtureIds {
      categoryIds: { manometers: string; transmitters: string };
      manufacturerIds: { wika: string; bd: string; metran: string };
      productIds: string[]; // 6 products: 3 in manometers, 3 in transmitters
      specFieldIds: { pressureMax: string; material: string; certified: string };
    }
    export async function seedPublicFixture(): Promise<PublicFixtureIds>;
    export async function teardownPublicFixture(ids: PublicFixtureIds): Promise<void>;
    ```

    Implementation requirements (single tx via `db.transaction`):
    - Insert 2 categories with 3 translation rows each. Slugs: uz `manometr` / `bosim-datchigi`; ru `manometry` / `datchiki-davleniya`; en `manometers` / `pressure-transmitters`.
    - Insert 3 manufacturers with 3 translations each (WIKA, BD Sensors, Метран). Use deterministic UUIDs (e.g. `'00000000-0000-4000-8000-00000000m001'` style) so e2e tests can hardcode them. Do NOT write `is_official_rep` (column does not exist yet in Wave 0).
    - Insert 3 spec_fields under the manometers category: `pressure_max` (kind=number, filter_kind=range, unit=`bar`), `material` (kind=enum, filter_kind=select, options uz/ru/en variants of "сталь/латунь/нерж."), `certified` (kind=bool, filter_kind=toggle).
    - Insert 6 products. SKUs: `M-100`, `M-200`, `M-300` (manometers); `T-100`, `T-200`, `T-300` (transmitters). Each product: `status='published'`, `manufacturerId` round-robin across 3 manufacturers, 3 translations (uz/ru/en) with name + slug + short_desc + long_desc.
    - For each manometer product, insert 3 product_spec_values: `pressure_max` num_value (100/250/600), `material` enum_value (steel/brass/inox), `certified` bool_value (true/false/true).
    - Do NOT write product_search rows (saveProduct's tsvector rebuild lands in plan 02 — tests that exercise SRCH-* will run that build first via `saveProduct`).
    - Do NOT write image_public_ids / datasheet_public_ids (columns land in plan 02).
    - `teardownPublicFixture` deletes by ID (FK cascades clean translations + spec values).

    Use `LOCALES = ['uz','ru','en']` from `@/i18n/routing` indirectly — or inline `const LOCALES = ['uz','ru','en'] as const;` to avoid an extra import.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/fixtures/seed-public.ts`
    - `grep -c 'export async function seedPublicFixture' tests/fixtures/seed-public.ts` returns 1
    - `grep -c 'export async function teardownPublicFixture' tests/fixtures/seed-public.ts` returns 1
    - `grep -q 'image_public_ids\|datasheet_public_ids\|is_official_rep' tests/fixtures/seed-public.ts` returns NOTHING (those columns land in plan 02)
    - `grep -c "M-100\|M-200\|M-300\|T-100\|T-200\|T-300" tests/fixtures/seed-public.ts` returns at least 6
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Deterministic public-side seed fixture compiles, exports the named factory + teardown, and writes only columns that exist in the Wave 0 schema.
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Create 11 RED test stubs (one per requirement file) + extend tests/actions/products.test.ts with SRCH-05 stub</name>
  <files>tests/lib/jsonld.test.ts, tests/lib/metadata.test.ts, tests/db/catalog.test.ts, tests/db/search.test.ts, tests/api/autocomplete.test.ts, tests/api/sitemap.test.ts, tests/e2e/locale-switcher.spec.ts, tests/e2e/category-nav.spec.ts, tests/e2e/catalog-filters.spec.ts, tests/e2e/product-detail.spec.ts, tests/e2e/manufacturers.spec.ts, tests/actions/products.test.ts</files>
  <read_first>
    - tests/lib/revalidation.test.ts (vitest unit shape — describe + vi.mock('next/cache'))
    - tests/actions/manufacturers.test.ts (live-Neon vitest shape)
    - tests/actions/products.test.ts (existing Phase-2 test file — APPEND new describe.skip block, do NOT replace any existing content)
    - tests/e2e/admin-edit-revalidates.spec.ts (Playwright e2e shape — BASE_URL fallback, page.goto, expect(...))
    - .planning/phases/03-public-rendering-search-seo/03-VALIDATION.md (per-requirement automated commands)
    - .planning/phases/03-public-rendering-search-seo/03-PATTERNS.md §"vitest unit test shape" + §"Playwright e2e test shape"
  </read_first>
  <action>
    Create each stub with `describe.skip(...)` (vitest) or `test.skip(...)` (Playwright) so the suite stays green while still showing the pending tests in output. Each stub MUST contain a TODO comment naming the requirement ID + the plan that will close it.

    **Vitest stubs** (use `describe.skip` so test count is reported but execution skipped):

    ```typescript
    // tests/lib/jsonld.test.ts
    import { describe, it, expect } from 'vitest';
    describe.skip('jsonld helpers (CAT-08, closed by plan 03)', () => {
      it('productJsonLd returns @type=Product without offers (D-08)', () => { /* TODO */ });
      it('organizationJsonLd returns @type=Organization', () => { /* TODO */ });
      it('breadcrumbJsonLd returns @type=BreadcrumbList with items', () => { /* TODO */ });
      it('collectionPageJsonLd returns @type=CollectionPage', () => { /* TODO */ });
    });
    ```

    Apply the same `describe.skip` shell to:
    - `tests/lib/metadata.test.ts` — describes for SEO-01 (hreflang map of uz/ru/en + x-default) and SEO-02 (canonical matches current locale URL). Closed by plan 03.
    - `tests/db/catalog.test.ts` — CAT-03 (product count for category) + CAT-04 (numeric range + enum + bool filter returns correct subset). Closed by plan 04.
    - `tests/db/search.test.ts` — SRCH-01 (ranked results in current locale) + SRCH-02 (zero-hit cascade uz→ru→en + banner data). Closed by plan 06.
    - `tests/api/autocomplete.test.ts` — SRCH-03 (prefix + SKU autocomplete; injection sanitization for `!&|()`). Closed by plan 06.
    - `tests/api/sitemap.test.ts` — SEO-03 (per-locale sitemap XML contains all published products + Content-Type application/xml). Closed by plan 08.

    **APPEND to `tests/actions/products.test.ts`** (do NOT remove or replace existing Phase-2 tests — just append a new describe.skip block at the end of the file):

    ```typescript
    // SRCH-05 stub — Plan 02 Task 2.3 will flip describe.skip → describe and add real assertions
    describe.skip('SRCH-05: saveProduct rebuilds product_search tsvector rows (closed by plan 02)', () => {
      it.skip('inserts 3 product_search rows (uz/ru/en) per saved product', () => { /* TODO */ });
      it.skip('rebuilds tsvectors when translations change', () => { /* TODO */ });
    });
    ```

    **Playwright stubs** (use `test.skip` so the spec compiles + lists in `--list`):

    ```typescript
    // tests/e2e/locale-switcher.spec.ts
    import { test, expect } from '@playwright/test';
    const baseURL = process.env.BASE_URL ?? 'http://localhost:3000';
    test.skip('CAT-01: clicking RU button navigates from /uz/... to /ru/... (closed by plan 03)', async ({ page }) => {
      // TODO
    });
    ```

    Apply the same `test.skip` shell to:
    - `tests/e2e/category-nav.spec.ts` — CAT-02 (left-rail tree renders, child links navigate). Closed by plan 03.
    - `tests/e2e/catalog-filters.spec.ts` — CAT-05 (filter selection updates URL; reload preserves selection). Closed by plan 04.
    - `tests/e2e/product-detail.spec.ts` — CAT-06 spec tables grouped, CAT-07 product name appears in raw HTML response (use `page.request.get` + body assertion), SRCH-04 visiting `/uz/search?q=M-100` 302-redirects to `/uz/products/<slug>`. Closed by plan 05 (CAT-06/07) + plan 06 (SRCH-04).
    - `tests/e2e/manufacturers.spec.ts` — MFG-01 index renders 3 manufacturer cards with `<img>`, MFG-02 detail page shows description + paginated product grid. Closed by plan 07.

    Each stub file MUST include `// REQUIRES: tests/fixtures/seed-public.ts seed must run before tests un-skip` comment near the top (does not apply to the products.test.ts append — that file already imports from seed-products fixture).
  </action>
  <verify>
    <automated>pnpm vitest run tests/lib/jsonld.test.ts tests/lib/metadata.test.ts tests/db/catalog.test.ts tests/db/search.test.ts tests/api/autocomplete.test.ts tests/api/sitemap.test.ts tests/actions/products.test.ts &amp;&amp; pnpm playwright test --list tests/e2e/locale-switcher.spec.ts tests/e2e/category-nav.spec.ts tests/e2e/catalog-filters.spec.ts tests/e2e/product-detail.spec.ts tests/e2e/manufacturers.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 standalone vitest stub files exist and `pnpm vitest run` reports them as `skipped` (not failed)
    - All 5 Playwright stub files exist and `pnpm playwright test --list` outputs them
    - `grep -l 'describe.skip\|test.skip' tests/lib/jsonld.test.ts tests/lib/metadata.test.ts tests/db/catalog.test.ts tests/db/search.test.ts tests/api/autocomplete.test.ts tests/api/sitemap.test.ts tests/e2e/locale-switcher.spec.ts tests/e2e/category-nav.spec.ts tests/e2e/catalog-filters.spec.ts tests/e2e/product-detail.spec.ts tests/e2e/manufacturers.spec.ts | wc -l` returns 11
    - `grep -q "describe.skip('SRCH-05" tests/actions/products.test.ts` (SRCH-05 stub appended; Plan 02 Task 2.3 flips it to describe with real assertions)
    - Existing Phase-2 tests in `tests/actions/products.test.ts` still pass — `pnpm vitest run tests/actions/products.test.ts` exits 0 (the appended describe.skip block does not break the existing 7 specs)
    - Every stub references its requirement ID in the describe/test title (e.g. `CAT-01:`, `SRCH-04:`, `SRCH-05:`)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    All 11 standalone stub files compile + skip; tests/actions/products.test.ts appended with SRCH-05 describe.skip block (Phase-2 tests preserved); full vitest run stays green; Playwright lists every spec; suite count grows but no RED tests yet (downstream plans flip the .skip → real assertions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → next.config.ts | Configuration change enables a new caching subsystem; misconfiguration could mask test failures or change rendering semantics admin-side |
| test fixture → Neon dev DB | Fixture writes deterministic seed data; if leaked into prod DB it pollutes content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | next.config.ts cacheComponents flag | mitigate | `pnpm build` after the flag enables on-demand validation that admin force-dynamic routes still render; CI pipeline catches build regressions before merge |
| T-03-01-02 | Tampering | tests/fixtures/seed-public.ts targeting prod DB | mitigate | Fixture uses `db` client which loads `DATABASE_URL` from `src/env`; fixture file lives under `tests/` and is never imported by production `src/` paths (verified by `grep -r 'seed-public' src/` returning empty); rely on existing CLAUDE.md guardrail "never `db:push` against prod" |
| T-03-01-03 | Information Disclosure | seed fixture exposing internal IDs | accept | UUIDs in tests are not secrets; deterministic IDs are required for cross-test stability |
</threat_model>

<verification>
- All 11 standalone test stub files exist and are skipped (not failed) in `pnpm vitest run` and `pnpm playwright test --list`
- `tests/actions/products.test.ts` appended with SRCH-05 describe.skip block; existing Phase-2 specs still GREEN
- `next.config.ts` has `cacheComponents: true`
- `package.json` declares `schema-dts` under dependencies
- `pnpm tsc --noEmit && pnpm build` exits 0
</verification>

<success_criteria>
Phase 3 downstream plans can now flip `describe.skip`/`test.skip` to active assertions and consume `seedPublicFixture()` for setup. Plan 02 Task 2.3 flips the appended SRCH-05 block in `tests/actions/products.test.ts` from describe.skip → describe with live-Neon assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-rendering-search-seo/03-01-SUMMARY.md` with a list of stub-to-plan mappings (which downstream plan closes each requirement) and a confirmation line that `pnpm build` exited 0 with `cacheComponents: true` enabled.
</output>
</content>
</invoke>