---
phase: 04-content-features
plan: 08
type: execute
wave: 2
depends_on: [04-02, 04-03, 04-06, 04-07]
files_modified:
  - src/components/admin/industry-body-editor.tsx
  - src/components/admin/industry-form.tsx
  - src/app/[locale]/admin/industries/page.tsx
  - src/app/[locale]/admin/industries/industries-table.tsx
  - src/app/[locale]/admin/industries/new/page.tsx
  - src/app/[locale]/admin/industries/[id]/edit/page.tsx
  - src/components/admin/sidebar.tsx
  - tests/components/industry-form.test.tsx
autonomous: true
requirements: [CONT-02, CONT-05]
must_haves:
  truths:
    - "IndustryBodyEditor mirrors RecipeBodyEditor verbatim — same useEditor + immediatelyRender: false + TIPTAP_EXTENSIONS + CldUploadWidget options.folder='industries'"
    - "IndustryForm uses LocaleTabs (Phase 2 primitive) swapping title/slug/excerpt/body per locale; non-translatable fields below; LinkedProductsPicker reused verbatim from 04-07 (no industry-specific picker)"
    - "Industries list page + lifecycle row actions (Save | Publish/Unpublish | Duplicate via ?duplicate=<id> | Delete) wire publishIndustry + unpublishIndustry + deleteIndustry"
    - "Sidebar navigation has new Industries entry"
    - "tests/components/industry-form.test.tsx flipped from RED to GREEN (2+ jsdom specs)"
  artifacts:
    - path: src/components/admin/industry-body-editor.tsx
      provides: "Tiptap admin editor for industry body (alias of RecipeBodyEditor with folder='industries')"
      contains: "folder: 'industries'"
    - path: src/components/admin/industry-form.tsx
      provides: "Single-page industry editor mirroring RecipeForm"
      contains: "LocaleTabs"
    - path: src/app/[locale]/admin/industries/page.tsx
      provides: "Industries list RSC + DataTable"
      contains: "findAllIndustriesForAdmin"
    - path: src/app/[locale]/admin/industries/industries-table.tsx
      provides: "Client island wrapping the industries DataTable"
      contains: "publishIndustry"
    - path: src/app/[locale]/admin/industries/new/page.tsx
      provides: "New industry RSC route"
      contains: "LinkedProductsPicker"
    - path: src/app/[locale]/admin/industries/[id]/edit/page.tsx
      provides: "Edit industry RSC route"
      contains: "saveIndustry"
    - path: src/components/admin/sidebar.tsx
      provides: "Sidebar gains Industries nav entry"
      contains: "/admin/industries"
  key_links:
    - from: src/components/admin/industry-form.tsx
      to: src/actions/industries.ts
      via: "saveIndustry(formValues) on submit"
      pattern: "saveIndustry"
    - from: src/components/admin/industry-body-editor.tsx
      to: src/lib/tiptap-extensions.ts
      via: "imports TIPTAP_EXTENSIONS"
      pattern: "TIPTAP_EXTENSIONS"
    - from: src/components/admin/industry-form.tsx
      to: src/components/admin/linked-products-picker.tsx
      via: "reuses LinkedProductsPicker verbatim from 04-07"
      pattern: "LinkedProductsPicker"
---

<objective>
This plan ships the industry admin UI surface. Mirrors plan 04-07 verbatim — same Tiptap editor pattern, same LocaleTabs swap, same LinkedProductsPicker (literally reused), same lifecycle row actions. Only difference: `options.folder='industries'` on the CldUploadWidget + entity name swaps.

LinkedProductsPicker is generic by design — accepts options as a prop. No industry-specific picker is needed; the Recipe form and Industry form import the SAME component from `src/components/admin/linked-products-picker.tsx`.

Parallelizable with 04-07 by file ownership (no overlap on industries-specific files; sidebar.tsx edit overlaps but the entries are additive — first-to-merge wins, second adds its entry on top).

Closure status: Standard execute plan. Flips 2 RED jsdom specs to GREEN. Exit gate: tsc clean + 2+ specs green + admin industries routes accessible.
</objective>

<tasks>

<task type="auto">
  <name>Task 8.1: IndustryBodyEditor (alias-thin-wrapper of RecipeBodyEditor) + IndustryForm</name>
  <files>src/components/admin/industry-body-editor.tsx, src/components/admin/industry-form.tsx</files>
  <action>
    Author `src/components/admin/industry-body-editor.tsx` mirroring `src/components/admin/recipe-body-editor.tsx` with one literal change: `options={{ folder: 'industries', resourceType: 'image' }}` (was 'recipes'). Otherwise identical.

    Optional refactor: extract a shared `BodyEditor` component taking `folder` as a prop. RESEARCH §Pitfalls / P4-3 calls out extension-drift between admin and public — same logic applies to per-entity editors. If the planner chooses to extract, BOTH RecipeBodyEditor + IndustryBodyEditor become 5-line wrappers calling `<BodyEditor folder="recipes" name="body.uz" />`. Defer this refactor IF context cost forces it; for v1 simplicity, keep two near-identical files (single point of duplication, well-tested).

    Author `src/components/admin/industry-form.tsx` mirroring `src/components/admin/recipe-form.tsx` from 04-07 verbatim — change `Recipe` → `Industry` and `recipeInsertSchema` → `industryInsertSchema`. The LinkedProductsPicker import path is unchanged (reused verbatim). The MediaUploader folder is `'industries'`. The submit handler calls `saveIndustry`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | grep -E "src/components/admin/industry" || echo "industry components typecheck"</automated>
  </verify>
  <done>IndustryBodyEditor + IndustryForm exported; both reference industry-specific Server Actions / Zod schemas / Cloudinary folder; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 8.2: Admin industries route files + sidebar entry</name>
  <files>src/app/[locale]/admin/industries/page.tsx, src/app/[locale]/admin/industries/industries-table.tsx, src/app/[locale]/admin/industries/new/page.tsx, src/app/[locale]/admin/industries/[id]/edit/page.tsx, src/components/admin/sidebar.tsx</files>
  <action>
    Author the 4 admin route files mirroring plan 04-07's recipe routes verbatim with industry entity swap. The findAllPublishedProducts pre-fetch is reused (same products list). The DataTable + lifecycle row actions wire publishIndustry / unpublishIndustry / deleteIndustry from src/actions/industries.ts.

    Update `src/components/admin/sidebar.tsx` adding an "Industries" nav entry beneath the Recipes entry (added in 04-07). Icon: lucide-react Factory or Building.

    Note: if 04-07 + 04-08 land in adjacent commits, the second plan's sidebar.tsx edit may rebase-conflict on the entries array — both edits append to the same JSX list. Resolve by including BOTH entries.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | grep -E "src/app/\[locale\]/admin/industries" || echo "admin industry routes typecheck"</automated>
  </verify>
  <done>4 admin route files exist; sidebar has Industries entry; lifecycle actions wired; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8.3: Flip industry-form jsdom RED specs to GREEN</name>
  <files>tests/components/industry-form.test.tsx</files>
  <behavior>
    - Spec 1 (renders with no hydration error): mirror recipe-form.test.tsx Spec 1 with industry entity
    - Spec 2 (LocaleTabs swap): switch tab from uz → ru, assert title/slug/excerpt/body inputs swap to ru-bound names
  </behavior>
  <action>
    Flip the 2 fixme specs in `tests/components/industry-form.test.tsx` from `test.fixme` to `test`. Implement assertions per `<behavior>`. Use vi.mock('next-cloudinary') + @testing-library/react.

    The LocaleTabs tab-swap spec specifically asserts the bug class P4-3 catches at the admin level (extension drift) by an analog (form-state drift across locales) — switching tabs MUST NOT lose state in the previously-displayed locale. RHF's per-path state is the structural defense; the spec is the regression-locker.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/industry-form.test.tsx</automated>
  </verify>
  <done>2 specs green; 0 fixme references; tsc clean.</done>
</task>

</tasks>

<dependencies>
- 04-02 — Tiptap deps + tiptap-extensions
- 04-03 — industryInsertSchema
- 04-06 — Server Actions for industries (saveIndustry + publish + unpublish + delete)
- 04-07 — LinkedProductsPicker (reused verbatim) + LocaleTabs/SlugInput patterns established (well, those came from Phase 2; 04-07 doesn't change them — but 04-07 establishes the recipe form shape that 04-08 mirrors)
</dependencies>

<verification>

```bash
pnpm tsc --noEmit
pnpm vitest run tests/components/industry-form.test.tsx
pnpm next dev   # smoke: visit /uz/admin/industries — list renders; click New, fill, save
```

After this plan ships, both content-tier admin surfaces are online. Wave 3 (public pages + Used-In integration) and Wave 4 (closure) can proceed.

</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Cloudinary direct (signed widget) | bytes never round-trip through Vercel; FOLDER_ALLOWLIST gate (04-02 added 'industries') |
| Admin form submit → saveIndustry / lifecycle actions | RHF + Zod validate client-side; Server Action re-validates with industryInsertSchema.parse() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| P4-1 | Tampering | RSC hydration mismatch on Tiptap admin editor (industry side) | mitigate | IndustryBodyEditor inherits immediatelyRender: false from RecipeBodyEditor pattern. |
| T-04-XSS-04 | XSS | Tiptap image data: URI bypass on industry side | mitigate | Same CloudinaryImage extension; admin inserts only via CldUploadWidget. |
| T-04-AUTH-01 | Spoofing | Direct saveIndustry call bypassing form | mitigate | withAdminAction wrap (Phase 2 + plan 04-06). |

</threat_model>

<deviations_protocol>

**Rule 1**: If sidebar.tsx edits from 04-07 + 04-08 conflict in a Wave 2 parallel-merge scenario, resolve by including BOTH the Recipes and Industries entries. This is a structural overlap, not a logic conflict.

**Rule 2**: If during implementation, a clear extraction opportunity surfaces (BodyEditor wrapping the per-folder difference), execute the extraction in this plan + a small refactor to update both 04-07's RecipeBodyEditor + 04-08's IndustryBodyEditor to call the shared component. Document the extraction in the SUMMARY.

**Rule 3**: If Wave 2 ships in non-parallel order (04-07 lands fully before 04-08 starts), the LinkedProductsPicker import is already known-good — no risk. If parallel, the Wave 2 critical-path test is whether `import { LinkedProductsPicker } from '@/components/admin/linked-products-picker'` resolves at the time 04-08 lands (it MUST, because 04-07 is a hard depends_on of 04-08).

</deviations_protocol>
